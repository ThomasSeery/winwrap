# winwrap

This project lives under `cpp/windows/`, the Windows-native area. The conventions
there (`cpp/windows/AGENTS.md`) layer on the shared `cpp/` rules. Load them first,
every session:

@../AGENTS.md

winwrap also has its own public-API conventions (config structs, field docs) on
top of those:

@CODE_CONVENTIONS.md
