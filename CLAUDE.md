# winwrap

This project lives under `cpp/windows/`, the Windows-native area. The conventions
there (`cpp/windows/CLAUDE.md`) layer on the shared `cpp/` rules. Load them first,
every session:

@../CLAUDE.md
